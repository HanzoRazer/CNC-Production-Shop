# Smart Guitar — CAD dimension sheet

Generated by `scripts/export_cad_dimensions.py` from the governed records.
Do not edit this file — edit the records and regenerate.

- Derived geometry: `fixtures/geometry/smart_guitar_cavity_geometry_v1.json`
- Component register: `fixtures/geometry/smart_guitar_component_register_v1.json`
- Placement: `CONF-POD-EMC-CLEARANCE`, re-derivable with
  `scripts/solve_khaya_pocket_layout.py`

All dimensions **mm**. Origin: body **top edge** for `y`, instrument
**centreline** for `x`, **+x toward treble**. Cavity `y` reads as
`-y_from_top`.

---

> ## DO NOT CUT FROM THIS SHEET
>
> `CONF-POD-EMC-CLEARANCE` is open. The back-face pocket positions
> in section 2 were solved against a stale void set — the design
> carries three voids, not four, and what the trace shows as a
> fourth is the electronics cavity itself. No valid placement has
> been derived since.
>
> Everything downstream of those positions — section 4 clearances,
> section 5 ribbon reach — is invalid with them. Sections 1 and 3
> are unaffected.

## 1. Body

| Dimension | Value |
|---|---:|
| Length | **468.5** |
| Width, maximum | **402.85** |
| Thickness | **47.0** |
| Required thickness | 41.0 (governed by `POD_HAT`) |
| Margin | 6.0 — **sufficient** |

Rim minimum 12.7, inter-cavity web minimum 8.0, floor minimum 8.0.

## 2. Back-face pockets

Sizes derived from the parts they hold; positions ruled for clearance.

| Pocket | Centre x | y from top | L | W | D | x extent | y extent |
|---|---:|---:|---:|---:|---:|---|---|
| `POD_PI` | **11.910** | **180.0** | 93.0 | 64.0 | 27.0 | -34.6 .. 58.4 | 148.0 .. 212.0 |
| `POD_HAT` | **94.410** | **280.0** | 73.0 | 64.5 | 33.0 | 57.9 .. 130.9 | 247.8 .. 312.2 |
| `BATTERY` | **2.910** | **247.5** | 90.0 | 55.0 | 21.0 | -42.1 .. 47.9 | 220.0 .. 275.0 |

## 3. Front-face features

| Feature | Centre x | y from top | L | W |
|---|---:|---:|---:|---:|
| `PU_BRIDGE` | 0.0 | 294.6 | 80.0 | 22.0 |
| `NECK_POCKET` | 0.0 | 93.1 | 76.8 | 55.2 |
| `BRIDGE` | 0.0 | 319.0 | 95.8 | 41.5 |
| `CONTROL` | 55.7 | 345.3 | 100.9 | 49.4 |

`NECK_POCKET`, `BRIDGE` and `CONTROL` are recovered from front_v5 with
both of its scale errors undone; they are keep-outs, not authored
geometry. `CONTROL` is disputed — see section 6.

## 4. Clearances — the revised measurements

Recomputed from the placement above, not quoted.

| From | To | Clearance |
|---|---|---:|
| `POD_PI` | `PU_BRIDGE` | **71.60** |
| `POD_PI` | `POD_HAT` | **35.75** |
| `POD_HAT` | `PU_BRIDGE` | **17.91** |
| `POD_HAT` | `BATTERY` | **10.00** |
| `POD_PI` | `BATTERY` | **8.00** |
| `BATTERY` | `PU_BRIDGE` | **8.60** |

Nearest keep-out for each pocket:

| Pocket | Rim | Nearest void | Nearest front-face feature |
|---|---:|---:|---|
| `POD_PI` | ok | 9.36 | `NECK_POCKET` 27.30 |
| `POD_HAT` | ok | 91.65 | `CONTROL_PLATE` 8.38 |
| `BATTERY` | ok | 9.88 | `BRIDGE_MOUNTING` 23.20 |

## 5. GPIO ribbon

| | Value |
|---|---:|
| Part length | 100.0 |
| Required run, header to header | **89.91** |
| Slack | 10.09 (10%) |

The run is measured header to header, not edge to edge. Each 40-pin
header sits at its pocket's x centre on an x-aligned edge, so a diagonal
layout costs far more ribbon than the pocket gap suggests — see
`CONF-RIBBON-REACH`, where an earlier layout needed 118.8 of a 100 part.

## 6. Unresolved

6 conflicts remain open against this geometry:

- **`CONF-BATTERY-PLACEMENT`** — electronics_components.battery_pack.location
- **`CONF-NVME-PLACEMENT`** — rear_electronics_cavity.contains[NVMe SSD]
- **`CONF-PICKUP-TYPE`** — luthiers-toolbox smart_guitar_v1.json: hardware.pickups vs cavities.teensy_io_pocket.signal_chain
- **`CONF-PICKUP-ROUTE-DIMS`** — pickup route dimensions
- **`CONF-POD-EMC-CLEARANCE`** — back-face pocket placement, ranked by clearance rather than by fit
- **`CONF-SINGLE-PICKUP-EMC`** — whether a single coil is viable 71.6 mm from a Raspberry Pi 5

